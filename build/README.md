`build-plugins.sh` lê `VERSION`, monta bundle único (skills + orquestrador + templates) e gera `dist/talos-{claude,codex}.plugin`.
Preenchida por S02.
Ver `_app-vault/docs/decisions/distribuicao.md` e backlog histórico em `.app-work/private/`.

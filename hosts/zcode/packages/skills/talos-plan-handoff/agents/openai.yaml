interface:
  display_name: "Talos Plan Handoff"
  short_description: "Handoff executável da família talos-* com prefixo e modo fechados"
  default_prompt: "Use $talos-plan-handoff para produzir um plano executável da cadeia talos-* com prefixo fechado, execution mode explícito, executor correto, validator interno obrigatório e review externo opcional."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Talos Task Validator"
  short_description: "Cold internal slice validator for talos-plan-execute"
  default_prompt: "Use $talos-task-validator to validate the executed plan slice against the plan contract and return structured findings with a deterministic verdict."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Talos Plan Execute"
  short_description: "Bounded plan execution with gates (talos-plan-execute)"
  default_prompt: "Use $talos-plan-execute to execute this handoff plan task by task with explicit invariants, focused validation, bounded self-repair, and clear stop conditions."

policy:
  allow_implicit_invocation: true
